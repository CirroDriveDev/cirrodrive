{
  "dependencies": {
    "@prisma/client": "^5.21.1"
  },
  "devDependencies": {
    "@cirrodrive/tsconfig": "workspace:^",
    "@node-rs/argon2": "^2.0.0",
    "@types/node": "^20.17.1",
    "mysql2": "^3.14.0",
    "prisma": "^5.21.1",
    "vite-node": "^2.1.4"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "name": "@cirrodrive/database",
  "prisma": {
    "seed": "vite-node prisma/seed.ts"
  },
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "clean": "git clean -xdf .cache .turbo node_modules",
    "db:generate": "prisma generate",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev --create-only --name init",
    "db:push": "prisma migrate reset --force --skip-seed && prisma db push --accept-data-loss --force-reset",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "dev": "prisma studio --browser none",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "healthcheck": "vite-node healthcheck.ts",
    "lint": "eslint"
  },
  "version": "0.0.0"
}
