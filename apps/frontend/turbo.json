{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["VITE_API_HOST", "VITE_API_PORT", "VITE_TOSS_CLIENT_KEY"]
    },
    "test": {
      "env": ["VITE_API_HOST", "VITE_API_PORT", "VITE_TOSS_CLIENT_KEY"]
    },
    "test:ci": {
      "env": ["VITE_API_HOST", "VITE_API_PORT", "VITE_TOSS_CLIENT_KEY"]
    }
  }
}
